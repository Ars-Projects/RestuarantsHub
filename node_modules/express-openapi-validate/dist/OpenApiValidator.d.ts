import { RequestHandler } from "express";
import OpenApiDocument, { Operation } from "./OpenApiDocument";
import Ajv = require("ajv");
export interface ValidatorConfig {
    ajvOptions?: Ajv.Options;
}
export interface PathRegexpObject {
    path: string;
    regex: RegExp;
}
export default class OpenApiValidator {
    private _ajv;
    private _document;
    constructor(openApiDocument: OpenApiDocument, options?: ValidatorConfig);
    validate(method: Operation, path: string): RequestHandler;
    match(): RequestHandler;
    validateResponse(method: Operation, path: string): (res: any) => void;
    private _getResponseObject;
    private _getPathItemObject;
    private _getOperationObject;
}
//# sourceMappingURL=OpenApiValidator.d.ts.map