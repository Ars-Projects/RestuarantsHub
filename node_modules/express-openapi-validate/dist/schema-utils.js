"use strict";
/*
  Copyright 2018 Santeri Hiltunen

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
*/
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const lodash_1 = __importDefault(require("lodash"));
const object_utils_1 = require("./object-utils");
const isReferenceObject = (x) => x.$ref !== undefined;
exports.resolveReference = (document, object) => {
    if (isReferenceObject(object)) {
        if (!object.$ref.startsWith("#/components/")) {
            throw new Error(`Unsupported $ref=${object.$ref}`);
        }
        const path = lodash_1.default.drop(object.$ref.split("/"), 1);
        const resolvedObject = lodash_1.default.get(document, path);
        if (resolvedObject === undefined) {
            throw new Error(`Object not found with $ref=${object.$ref}`);
        }
        return resolvedObject;
    }
    return object;
};
const arrayFields = ["allOf", "anyOf", "oneOf"];
const schemaFields = ["items", "not", "additionalProperties"];
exports.walkSchema = (originalSchema, mapper) => {
    let schema = mapper(originalSchema);
    const walk = (s) => exports.walkSchema(s, mapper);
    if (schema.properties !== undefined) {
        schema = { ...schema, properties: lodash_1.default.mapValues(schema.properties, walk) };
    }
    arrayFields
        .filter(f => f in schema)
        .forEach(f => {
        schema = { ...schema, [f]: schema[f].map(walk) };
    });
    schemaFields
        .filter(f => f in schema)
        .forEach(f => {
        const nestedSchema = schema[f];
        if (f === "additionalProperties" && typeof nestedSchema === "boolean") {
            return;
        }
        schema = { ...schema, [f]: walk(nestedSchema) };
    });
    return schema;
};
exports.mapOasSchemaToJsonSchema = (originalSchema, document) => {
    const mapOasFieldsToJsonSchemaFields = (s) => {
        let schema = exports.resolveReference(document, s);
        if (Array.isArray(schema.type)) {
            throw new TypeError("Type field in schema must not be an array");
        }
        if (Array.isArray(schema.items)) {
            throw new TypeError("Items field in schema must not be an array");
        }
        // Need to figure out how to handle the case when nullable is false and
        // there's no type specified. The OAS spec isn't explicit about that corner
        // case. Setting type to an array with all the primitive types except null
        // is one option but doesn't seem right. Do nothing for now.
        if (schema.nullable === true && typeof schema.type === "string") {
            schema = { ...schema, type: [schema.type, "null"] };
        }
        schema = object_utils_1.dissoc(schema, "nullable");
        return schema;
    };
    const jsonSchema = exports.walkSchema(originalSchema, mapOasFieldsToJsonSchemaFields);
    jsonSchema.$schema = "http://json-schema.org/draft-04/schema#";
    return jsonSchema;
};
exports.oasPathToExpressPath = (path) => path.replace(/\{([^}]+)\}/g, ":$1");
//# sourceMappingURL=schema-utils.js.map